//write a program that display character length using string
#include <stdio.h>
#include <string.h>
int main ()
{
    char s1[]= "Ajmain Nishu";

    int len = strlen(s1);

    printf("Length: %d\n", len);

    return 0;
}
